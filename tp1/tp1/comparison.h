

/*
 * ceci est la fonction qu'on devrait appeller dans le cas ou on veut comparer deux valeur de type float.
 * initial_value : la valeur initiale qu'on doit comparer à la racine.
 * root : la racine d'un nombre.
 * return : la difference .
 */
float comparison(float initial_value, float root);
/*
 * fonction conparison_absikutie qu'on devrait appeller dans le cas ou on veut comparer deux valeur de type float.
 * initial_value : la valeur initiale qu'on doit comparer à la racine.
 * root : la racine d'un nombre.
 * return : la difference .
 */
float comparison_absolutie(float initial_value, float root);

